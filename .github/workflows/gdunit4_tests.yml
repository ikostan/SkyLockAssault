---
name: "GDUnit4 Unit Tests & Code Coverage"

# Ref #889, #887: Standardizes Godot 4.7.1-stable runtime, gdUnit4 v6.2.0, and gdUnit4-coverage v0.1.4.
# Implements micro-batched LCOV execution, tracefile merging, and Codecov upload with --flag GDUnit4.
on:  # yamllint disable-line rule:truthy
  workflow_call:
    inputs:
      godot_version:
        type: "string"
        default: "4.7.1-stable"
      godot_sha256:
        type: "string"
        default: "c7ff14fd28472c8d4f193043de30278dcf7e5241a1dcf7566b02e27addaa33ba"
    secrets:
      CODECOV_TOKEN:
        description: "Codecov requires a token to authenticate"
        required: false

jobs:
  unit-test:
    runs-on: "ubuntu-latest"
    timeout-minutes: 15
    permissions:
      checks: "write"
    env:
      GODOT_VERSION: "${{ inputs.godot_version }}"
      GODOT_SHA256: "${{ inputs.godot_sha256 }}"
    steps:
      # Check out full repository history (fetch-depth: 0) to allow Codecov base commit resolution
      - uses: "actions/checkout@v7"
        with:
          persist-credentials: false
          fetch-depth: 0

      # Install virtual frame buffer (xvfb) for display emulation and lcov for tracefile merging
      - name: "Install System Dependencies (xvfb & lcov)"
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb lcov

      # Download official Godot Linux binary and enforce SHA-256 integrity validation
      - name: "Download and Verify Godot Binary"
        run: |
          URL="https://github.com/godotengine/godot/releases/download"
          ZIP_NAME="Godot_v${GODOT_VERSION}_linux.x86_64.zip"
          wget -q "${URL}/${GODOT_VERSION}/${ZIP_NAME}"

          echo "=================================================="
          echo "         ASSET SHA256 CHECKSUM                    "
          echo "=================================================="
          CALCULATED_SHA256=$(sha256sum "${ZIP_NAME}" | awk '{print $1}')
          echo "SHA256 for ${ZIP_NAME}:"
          echo "${CALCULATED_SHA256}"
          echo "=================================================="

          # Verify downloaded asset checksum against expected hash if provided
          if [ -n "${GODOT_SHA256}" ]; then
            echo "${GODOT_SHA256}  ${ZIP_NAME}" | sha256sum --check --status || {
              echo "❌ CRITICAL: Godot binary verification failed! Checksum mismatch."
              exit 1
            }
          fi

          unzip "${ZIP_NAME}"
          mv "Godot_v${GODOT_VERSION}_linux.x86_64" godot
          chmod +x godot

      # Validate that downloaded binary matches the expected version string (converts -stable to .stable)
      - name: "Verify Godot Engine Version Parity"
        run: |
          ENGINE_VERSION=$(./godot --version)
          echo "Detected engine version: $ENGINE_VERSION"
          EXPECTED_VERSION="${GODOT_VERSION/-stable/.stable}"
          echo "$ENGINE_VERSION" | grep -q "$EXPECTED_VERSION" || {
            echo "❌ ERROR: Expected Godot ${EXPECTED_VERSION}, but received: $ENGINE_VERSION"
            exit 1
          }
          echo "✅ Version parity verified (${EXPECTED_VERSION})."

      # Install GDUnit4 Core addon, GDUnit4 Coverage GDExtension, and enable plugin in project.godot
      - name: "Install GDUnit4 Core & Coverage GDExtension"
        run: |
          rm -rf addons/gdUnit4 addons/gdunit4_coverage temp_cov override.cfg
          mkdir -p addons

          # 1. Download and extract GDUnit4 Core framework (v6.2.0)
          URL_GDUNIT="https://github.com/godot-gdunit-labs/gdUnit4/archive/refs/tags/v6.2.0.zip"
          wget -q "$URL_GDUNIT" -O gdunit4.zip
          unzip -q gdunit4.zip -d addons
          mv addons/gdUnit4-6.2.0/addons/gdUnit4 addons/gdUnit4
          rm -rf addons/gdUnit4-6.2.0 gdunit4.zip

          # 2. Download and unpack GDUnit4 Coverage GDExtension (v0.1.4)
          URL_COV="https://github.com/godot-gdunit-labs/gdUnit4-coverage/releases/download/v0.1.4/gdUnit4-coverage-0.1.4-trial.zip"
          wget -q "$URL_COV" -O coverage.zip
          mkdir -p temp_cov
          unzip -q coverage.zip -d temp_cov/
          rm coverage.zip

          cp -r temp_cov/addons/gdunit4_coverage addons/
          rm -rf temp_cov

          # 3. Safely inject plugin activation and disable strict warnings to avoid test aborts
          python3 -c '
          with open("project.godot", "r") as f:
              content = f.read()

          if "res://addons/gdunit4_coverage/plugin.cfg" not in content:
              if "enabled=PackedStringArray(" in content:
                  content = content.replace("enabled=PackedStringArray(", "enabled=PackedStringArray(\"res://addons/gdunit4_coverage/plugin.cfg\", ")
              elif "[editor_plugins]" in content:
                  content = content.replace("[editor_plugins]", "[editor_plugins]\n\nenabled=PackedStringArray(\"res://addons/gdunit4_coverage/plugin.cfg\")")
              else:
                  content += "\n\n[editor_plugins]\n\nenabled=PackedStringArray(\"res://addons/gdunit4_coverage/plugin.cfg\")\n"

          if "[debug]" in content:
              content = content.replace("[debug]", "[debug]\n\ngdscript/warnings/enable=false")
          else:
              content += "\n\n[debug]\n\ngdscript/warnings/enable=false\n"

          with open("project.godot", "w") as f:
              f.write(content)
          '

          if [ -f "./addons/gdunit4_coverage/gdunit4_coverage.gdextension" ]; then
            echo "✅ GDExtension successfully installed."
          else
            echo "❌ ERROR: Failed to install gdunit4_coverage GDExtension!"
            exit 1
          fi

      # Dynamically query manifest.json to resolve and download the matching gdcov coverage runner binary
      - name: "Download and Setup gdcov Runner"
        run: |
          wget -q "https://github.com/godot-gdunit-labs/gdUnit4-coverage/releases/download/v0.1.4/manifest.json" -O manifest.json

          # Resolve matching platform binary URL dynamically by matching major.minor version prefix
          GDCOV_URL=$(python3 -c '
          import json, os
          with open("manifest.json") as f:
              m = json.load(f)

          gdcov = m.get("gdcov", {})
          godot_ver = os.environ.get("GODOT_VERSION", "4.7.1-stable")
          major_minor = ".".join(godot_ver.split(".")[:2])
          url = ""

          # Match exact version key or major.minor prefix (e.g., "4.7-stable")
          for ver_key, platforms in gdcov.items():
              if ver_key == godot_ver or ver_key.startswith(major_minor):
                  linux_info = platforms.get("linux-x86_64") or platforms.get("linux", {})
                  if isinstance(linux_info, dict):
                      url = linux_info.get("url", "")
                      if url:
                          break

          print(url)
          ')

          if [ -z "$GDCOV_URL" ]; then
            echo "❌ Failed to resolve gdcov download URL from manifest.json"
            cat manifest.json
            exit 1
          fi

          # Extract into an isolated folder to avoid picking up repo binaries
          mkdir -p temp_gdcov
          wget -q "$GDCOV_URL" -O gdcov_linux.zip
          unzip -q gdcov_linux.zip -d temp_gdcov/
          RUNNER=$(find temp_gdcov/ -type f -name "gdcov*" | head -n 1)
          mv "$RUNNER" ./gdcov
          rm -rf temp_gdcov gdcov_linux.zip manifest.json
          chmod +x ./gdcov
          echo "✅ gdcov runner ready: $(./gdcov --version)"

      # Move tests outside workspace to prevent Godot from compiling full test tree upfront on boot
      - name: "Isolate Test Suites Outside Workspace"
        run: |
          # 1. Back up all test suites to /tmp outside the project res:// root
          rm -rf /tmp/test_backup
          cp -r test /tmp/test_backup

          # 2. Clear res://test completely to ensure a clean state for micro-batching
          rm -rf test
          mkdir -p test/gdunit4

          # 3. Configure source inclusion and test exclusion filters in override.cfg
          cat << 'EOF' > override.cfg
          [gdunit4]
          coverage/include_paths=PackedStringArray("res://scripts")
          coverage/exclude_paths=PackedStringArray("res://test", "res://addons")

          [gdunit4_coverage]
          runner/gdcov_path="./gdcov"
          settings/include_filters=PackedStringArray("res://scripts")
          settings/exclude_filters=PackedStringArray("res://test", "res://addons")
          EOF

          echo "✅ Test suites stashed to /tmp/test_backup and override.cfg configured."

      # Initialize Godot project and generate .godot cache to register newly installed GDExtensions
      - name: "Initialize Project and Register GDExtensions"
        run: |
          rm -rf .godot/
          ./godot --headless --editor --quit --path .

      # Run test suites in isolated micro-batches (2 suites per batch) with scoped include targets
      - name: "Run GDUnit4 Coverage in Micro-Batches"
        run: |
          rm -rf lcov_batches
          mkdir -p lcov_batches

          mapfile -t TEST_FILES < <(find /tmp/test_backup/gdunit4 -name "test_*.gd" ! -name "test_helpers.gd" | sort)
          echo "Found ${#TEST_FILES[@]} test suites. Running micro-batches..."

          BATCH_NUM=1
          CHUNK_SIZE=2

          for ((i=0; i<${#TEST_FILES[@]}; i+=CHUNK_SIZE)); do
            CHUNK=("${TEST_FILES[@]:i:CHUNK_SIZE}")
            echo "--------------------------------------------------"
            echo "🚀 Executing Micro-Batch $BATCH_NUM: ${CHUNK[*]}"
            echo "--------------------------------------------------"

            # Reset workspace and stage ONLY current batch test files + shared helper
            rm -rf test
            mkdir -p test/gdunit4

            if [ -f "/tmp/test_backup/gdunit4/test_helpers.gd" ]; then
              cp "/tmp/test_backup/gdunit4/test_helpers.gd" "test/gdunit4/"
            fi

            ADD_ARGS=()
            for t_file in "${CHUNK[@]}"; do
              BASE_NAME=$(basename "$t_file")
              cp "$t_file" "test/gdunit4/$BASE_NAME"
              ADD_ARGS+=(--add "res://test/gdunit4/$BASE_NAME")
            done

            # Explicitly scope include paths under 20 files based on domain being tested
            if [[ "${CHUNK[*]}" =~ (player|bullet|weapon|difficulty) ]]; then
              INCLUDE_TARGETS='PackedStringArray("res://scripts/entities", "res://scripts/core", "res://scripts/resources")'
            elif [[ "${CHUNK[*]}" =~ (audio_manager|parallax|bridge) ]]; then
              INCLUDE_TARGETS='PackedStringArray("res://scripts/managers", "res://scripts/system", "res://scripts/core", "res://scripts/resources")'
            elif [[ "${CHUNK[*]}" =~ (settings|input_remap|globals) ]]; then
              INCLUDE_TARGETS='PackedStringArray("res://scripts/core", "res://scripts/resources", "res://scripts/ui/components")'
            else
              INCLUDE_TARGETS='PackedStringArray("res://scripts/ui", "res://scripts/core")'
            fi
            
            # Write valid INI config starting at column 0 (avoids YAML indentation corruption)
            # and injects batch-scoped include filters to stay below the 20-file trial cap
            printf "[gdunit4]\ncoverage/include_paths=%s\ncoverage/exclude_paths=PackedStringArray(\"res://test\", \"res://addons\")\n\n[gdunit4_coverage]\nrunner/gdcov_path=\"./gdcov\"\nsettings/include_filters=%s\nsettings/exclude_filters=PackedStringArray(\"res://test\", \"res://addons\")\n" "$INCLUDE_TARGETS" "$INCLUDE_TARGETS" > override.cfg

            # Remove stale runner coverage tracefiles prior to batch launch
            find "$HOME/.local/share/godot/" "$PWD" -name "coverage.lcov" -delete 2>/dev/null || true

            # Execute micro-batch under virtual display using gdcov runner
            xvfb-run ./gdcov -d --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd \
              --ignoreHeadlessMode \
              "${ADD_ARGS[@]}"

            # Save batch coverage tracefile
            USER_PATH=$(find "$HOME/.local/share/godot/" "$PWD" -name "coverage.lcov" 2>/dev/null | head -n 1)
            if [ -n "$USER_PATH" ] && [ -f "$USER_PATH" ]; then
              cp "$USER_PATH" "lcov_batches/batch_${BATCH_NUM}.lcov"
              echo "✅ Captured tracefile: lcov_batches/batch_${BATCH_NUM}.lcov"
            else
              echo "⚠️ WARNING: No tracefile generated for Batch $BATCH_NUM"
            fi

            BATCH_NUM=$((BATCH_NUM + 1))
          done

      - name: "Debug Batch Tracefiles"
        run: |
          echo "=== Checking each batch for scripts/entities and scripts/system ==="
          for f in lcov_batches/*.lcov; do
            echo "--- $f ---"
            grep '^SF:.*entities' "$f" || echo "No entities in $f"
            grep '^SF:.*system' "$f" || echo "No system in $f"
          done

      # Restore full test folder unconditionally so downstream report finders and artifact uploaders have complete access
      - name: "Restore Full Test Suite"
        if: "always()"
        run: |
          if [ -d "/tmp/test_backup" ]; then
            rm -rf test
            cp -r /tmp/test_backup test
            echo "✅ Full test suite workspace restored from /tmp/test_backup."
          fi

      # Aggregate micro-batch coverage tracefiles into a single unified report and sanitize filepaths
      - name: "Merge LCOV Tracefiles"
        if: "success()"
        run: |
          LCOV_ARGS=()
          for f in lcov_batches/*.lcov; do
            if [ -f "$f" ]; then
              LCOV_ARGS+=(-a "$f")
            fi
          done

          if [ ${#LCOV_ARGS[@]} -gt 0 ]; then
            lcov "${LCOV_ARGS[@]}" -o final_coverage.lcov
          else
            echo "❌ ERROR: No batch LCOV tracefiles were captured."
            exit 1
          fi

          # 1. Sanitize file paths
          sed -i 's|SF:.*/SkyLockAssault/|SF:|g' final_coverage.lcov
          sed -i 's|SF:res://|SF:|g' final_coverage.lcov
          sed -i 's|^SF:/*|SF:|g' final_coverage.lcov

          # 2. Extract ONLY production scripts (strips test/gdunit4/ from coverage)
          lcov --extract final_coverage.lcov "scripts/*" -o final_coverage.lcov

          # 3. Clean up batch directory
          rm -rf lcov_batches

          echo "=== Final Filtered Production LCOV Summary ==="
          lcov --summary final_coverage.lcov

      # Locate most recent XML test report directory generated by GDUnit4
      - name: "Find Latest Report Directory"
        id: "find_report"
        if: "always()"
        run: |
          LATEST_REPORT=$(find reports artifacts/gdunit-reports -maxdepth 1 -type d -name 'report_*' -printf '%T@\t%p\n' 2>/dev/null | sort -rn | head -n 1 | cut -f2-)
          echo "latest_report=$LATEST_REPORT" >> "$GITHUB_OUTPUT"

      # Upload XML test results and final LCOV coverage file as GitHub Actions run artifacts
      - name: "Upload Test Reports Artifacts"
        if: "always()"
        uses: "actions/upload-artifact@v7"
        with:
          name: "gdunit-reports"
          path: |
            reports/**
            artifacts/gdunit-reports/**
            final_coverage.lcov
          if-no-files-found: "ignore"
          retention-days: 5

      - name: "Inspect LCOV File Paths"
        run: |
            echo "=== Sample SF paths in final_coverage.lcov ==="
            grep '^SF:' final_coverage.lcov | head -n 30

      # Install standalone Codecov CLI binary (guarded inside script when token is empty)
      - name: "Install Codecov CLI"
        if: "success()"
        env:
          CODECOV_TOKEN: "${{ secrets.CODECOV_TOKEN }}"
        run: |
          if [ -z "$CODECOV_TOKEN" ]; then
            echo "⚠️ CODECOV_TOKEN not provided; skipping Codecov CLI installation."
            exit 0
          fi
          curl -Os https://cli.codecov.io/latest/linux/codecov
          chmod +x codecov

      # Upload LCOV coverage report and XML test results to Codecov (guarded for unauthenticated fork runs)
      - name: "Upload LCOV Coverage & Test Results to Codecov"
        if: "success()"
        env:
          CODECOV_TOKEN: "${{ secrets.CODECOV_TOKEN }}"
        run: |
          if [ -z "$CODECOV_TOKEN" ]; then
            echo "⚠️ CODECOV_TOKEN not provided; skipping Codecov upload."
            exit 0
          fi

          echo "=================================================="
          echo "         CODECOV UPLOAD DIAGNOSTICS               "
          echo "=================================================="

          # 1. Upload unified LCOV coverage tracefile
          if [ -f "./final_coverage.lcov" ]; then
            echo "✅ Found ./final_coverage.lcov ($(du -h ./final_coverage.lcov | cut -f1))"
            echo "🚀 Initiating Codecov coverage upload..."
            ./codecov --verbose upload-process \
              --token "$CODECOV_TOKEN" \
              -f "./final_coverage.lcov" \
              --disable-search \
              --name "GDUnit4 Code Coverage" \
              --flag GDUnit4
          else
            echo "❌ CRITICAL: ./final_coverage.lcov WAS NOT FOUND!"
          fi

          echo "--------------------------------------------------"

          # 2. Upload test results XML report if available
          REPORT_DIR="${{ steps.find_report.outputs.latest_report }}"
          REPORT_FILE=""
          if [ -f "$REPORT_DIR/results.xml" ]; then
            REPORT_FILE="$REPORT_DIR/results.xml"
          elif [ -f "$REPORT_DIR/test-report.xml" ]; then
            REPORT_FILE="$REPORT_DIR/test-report.xml"
          fi

          if [ -n "$REPORT_FILE" ]; then
            echo "✅ Found test report XML at: $REPORT_FILE"
            echo "🚀 Uploading test result XML to Codecov..."
            ./codecov --verbose upload-process \
              --token "$CODECOV_TOKEN" \
              -f "$REPORT_FILE" \
              --disable-search \
              --name "GDUnit4 Test Report" \
              --flag GDUnit4 \
              --report-type test_results
          else
            echo "⚠️ WARNING: Test report XML not found in '$REPORT_DIR'"
          fi

          echo "=================================================="
